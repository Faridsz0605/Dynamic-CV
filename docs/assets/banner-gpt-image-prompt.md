# GPT Image banner prompt

Use this prompt with the GPT image model when an image-generation API key is available. Replace `docs/assets/banner.svg` with the generated image if the output is better than the current SVG fallback.

```text
Create a wide GitHub README banner for a software project named "Farid Sayago — Dynamic AI Portfolio".

Style: dark blue terminal aesthetic, glass panels, subtle grid, clean MLOps/data engineering mood, premium recruiter-facing portfolio, not cartoonish. Include abstract data pipeline lines, a small terminal window, AI/RAG nodes, and a quiet Colombian/Latin American reference through color warmth only, no flags.

Text to include exactly:
Farid Sayago
Dynamic AI Portfolio
MLOps résumé · RAG chat · voice mode · evals · ops telemetry

Aspect ratio: 16:5. No logos from existing companies. No fake UI text. Leave safe margins for GitHub README display.
```
